package com.example.CJLInvestimentos.entities.enums;

public enum TipoAlerta {
    /** Alertar quando o preco atingir ou ultrapassar o valor definido. */
    ACIMA,
    /** Alertar quando o preco cair abaixo do valor definido. */
    ABAIXO
}
